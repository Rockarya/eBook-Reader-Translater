LANGUAGES = ['eng','hin','tel']
# These language codes will be used directly for the API so please ensure they match the API's documentation for languages codes.
# To add a language, simply add the language code to the above list.